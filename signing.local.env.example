# Copy to signing.local.env (gitignored) and fill in values.
# Source before release builds:
#   set -a && source signing.local.env && set +a
#
export KEY_ALIAS=fileapex-alias
export KEYSTORE_PASSWORD=
export KEY_PASSWORD=
